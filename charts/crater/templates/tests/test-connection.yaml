apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crater.fullname" . }}-test-connection"
  labels:
    {{- include "crater.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crater.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
