apiVersion: v1
kind: Service
metadata:
  name: {{ include "crater.fullname" . }}
  labels:
    {{- include "crater.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crater.selectorLabels" . | nindent 4 }}
